{
  "name": "@nmt/delivery-coding-task-backend",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "build": "rimraf dist && ./node_modules/.bin/tsc",
    "start": "pnpm build && node dist/main.js",
    "watch": "nodemon --watch src -e ts --exec 'pnpm start'"
  },
  "dependencies": {
    "express": "^4.18.0",
    "typescript": "^5.2.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.0",
    "concurrently": "^8.2.0",
    "nodemon": "^3.0.0",
    "rimraf": "^5.0.0"
  }
}
